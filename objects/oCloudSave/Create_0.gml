/// @desc Setup

/*
  Ensure you have enabled "Enable Google Cloud Saving" in Game Options > Android 
  and added your own unique ID there, otherwise this save/load process will always fail
*/


// This event only exists so we could include the comment above, so
// just inherit the parent event now (you could delete this whole Create event if you wished)
event_inherited();
