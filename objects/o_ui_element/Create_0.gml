/// @description UI Element Initializing
ui_x = 0;
ui_y = 0

is_active = true;

camera = view_camera[0];

camera_x = 0;
camera_y = 0;

camera_width = camera_get_view_width(camera);
camera_height = camera_get_view_height(camera);